SQLAlchemy==1.4.50
numpy==1.26
PyYAML==6.0.1
requests>=2.0.0
pandas>=2.1.3
traitlets-pcse==5.0.0.dev
matplotlib==3.8.1
nlopt==2.7.1
# 1.3.12 has no wheel for Python 3.12 and its legacy source build fails with
# modern setuptools (pkg_resources is unavailable in the isolated build env).
# 1.4.8 still supports the SALib.sample.saltelli API used by notebook 10.
salib==1.4.8
pcse==6.0.13
dotmap>=1.3
openpyxl==3.1.2
